services:
  - type: web
    name: pet_HMS_frontend
    env: static
    buildCommand: cd frontend/main-app && npm install && npm run build
    staticPublishPath: frontend/main-app/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html